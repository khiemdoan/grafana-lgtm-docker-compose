apiVersion: 1

providers:
  - name: "RED Metrics (classic histogram)"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/red-metrics-classic.json
      foldersFromFilesStructure: false
  - name: "RED Metrics (exponential/native histogram)"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/red-metrics-native.json
      foldersFromFilesStructure: false
  - name: "JVM Metrics"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/jvm-metrics.json
      foldersFromFilesStructure: false
